import { Button, message, Input, Modal, Radio, Form } from 'ant-design-vue';
import 'ant-design-vue/dist/antd.css';
import { register } from './register';

register(Button, message, Input, Modal, Radio, Form);
